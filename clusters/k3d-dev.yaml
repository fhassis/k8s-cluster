apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: k3d-dev
servers: 1
agents: 0 # 0 agents = single node (server acts as worker)
image: rancher/k3s:latest
options:
  k3d:
    wait: true
    timeout: "60s"
  kubeconfig:
    updateDefaultKubeconfig: true
    switchCurrentContext: true
